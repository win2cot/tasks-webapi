# 基本設計書

タスク管理システム(マルチテナントSaaS)

Version 1.3.1

2026年5月10日

作成者: 開発チーム

## 改訂履歴

| 版数 | 改訂日 | 改訂内容 | 改訂者 |
|---|---|---|---|
| 1.0 | 2026-05-05 | 新規作成 | 開発チーム |
| 1.1 | 2026-05-05 | Java21/SpringBoot4/MySQL/Gradleへ更新、マルチテナントSaaS化、Keycloak採用、NIST準拠 | 開発チーム |
| 1.2 | 2026-05-05 | クリーンアーキ採用、ECS Fargate、tasks(due_date必須/owner/visibility)、task_stakeholders追加、カテゴリ/チーム/Viewer除外 | 開発チーム |
| 1.3 | 2026-05-06 | レビュー指摘 M-1〜M-3 反映: §4.2.7 user_notification_settings / §4.2.8 shedlock 追加、§6.2.2 ダッシュボード集計の認可スコープ追加、§6.2.3 認可違反時の応答ポリシー(404/403)追加 | 開発チーム |
| 1.3.1 | 2026-05-10 | PR #76 自動レビュー指摘の必須対応: §4.2.3 user_tenants の VIEWER 表記削除(要件定義書 §2.3 整合)。OpenAPI側で 401/403 全エンドポイント網羅、Stakeholder.addedBy 追加、TaskUpdateRequest description 補強、user_notification_settings API 追加、SaaS Admin 認可モデル詳細を info.description に追記 | 開発チーム |

## 目次

1. はじめに

1.1 本書の位置付け

1.2 関連文書

2. システム構成

2.1 アーキテクチャ概要(クリーンアーキテクチャ)

2.1.1 クリーンアーキテクチャ層構成

2.2 論理構成図

2.3 物理構成図(ECS on Fargate)

2.4 ソフトウェア構成

3. 画面設計

3.1 画面遷移図

3.2 画面一覧

3.3 主要画面仕様

4. データベース設計

4.1 ER図

4.2 テーブル定義(4.2.1 users / 4.2.2 tenants / 4.2.3 user_tenants / 4.2.4 tasks / 4.2.5 task_stakeholders / 4.2.6 audit_logs / 4.2.7 user_notification_settings / 4.2.8 shedlock)

4.3 インデックス設計

5. API設計

5.1 API一覧

5.2 認証方式

5.3 主要APIスキーマ

5.4 エラー応答

6. セキュリティ設計(NIST準拠)
  6.1 NIST統制マッピング
  6.2 認証・認可(6.2.1 タスクごとの認可ロジック / 6.2.2 ダッシュボード集計の認可スコープ / 6.2.3 認可違反時の応答ポリシー)
  6.3 マルチテナント分離
  6.4 入力検証・出力エンコーディング
  6.5 通信・データ保護
  6.6 CSRF/CORS
  6.7 監査ログの改ざん検知

7. 例外・ログ設計

8. バッチ設計

## 1. はじめに

### 1.1 本書の位置付け

本書は、要件定義書(Version 1.0)を入力とし、タスク管理システムの基本設計を記述する。本書はシステムの構造・画面・データ・APIの全体像を提示し、詳細設計および実装の前提となる。

### 1.2 関連文書

- 要件定義書 Version 1.0(2026-05-05)
- 開発計画書 Version 1.0(2026-05-05)
- コーディング規約(別添、本フェーズで策定予定)

## 2. システム構成

### 2.1 アーキテクチャ概要

本システムはクリーンアーキテクチャ(Domain / UseCase / Adapter / Infrastructure の4層)を採用するマルチテナントSaaSである。依存関係は外側から内側にのみ流れ、Domain層はフレームワーク非依存である。フロントエンドはHTML/CSS/JavaScriptによるSPA風の単一ページ構成とし、バックエンドはJava 21 / Spring Boot 4で構築するREST APIを提供する。認証・認可はKeycloak(OIDC)に委譲し、データはMySQL 8.xに永続化する。実行基盤はAmazon ECS on AWS Fargate(EC2は使用しない)。テナント分離は共用DB・共用スキーマ方式とし、全業務テーブルにtenant_id列を持つ。

### 2.1.1 クリーンアーキテクチャ層構成

| 層 | パッケージ例 | 責務 |
|---|---|---|
| Domain | com.example.task.domain | エンティティ、値オブジェクト、ドメインサービス、リポジトリI/F。Spring非依存 |
| UseCase (Application) | com.example.task.usecase | ユースケース実装、入出力Port定義、トランザクション境界 |
| Adapter | com.example.task.adapter.web / .persistence / .external | REST Controller、JPA Repository実装、Keycloak/SES クライアント |
| Infrastructure | com.example.task.infra | Spring Boot設定、DataSource、SecurityConfig、起動用Application |

- 依存方向: Infrastructure → Adapter → UseCase → Domain(逆方向の参照は禁止)
- Domain は POJO のみ。JPA アノテーションは Adapter.persistence の Entity クラスに記述
- DTO ↔ Domain の変換は Adapter 層の Mapper(MapStruct使用)で行う
- テストは Domain / UseCase が単体テストの主対象。Adapter は結合テスト中心

### 2.2 論理構成図

以下に論理構成を示す(ASCII表記)。

┌────────────────────────────────────────────────────────────┐

│                  ブラウザ (Chrome/Edge 等)                  │

│  HTML / CSS / JavaScript (Bootstrap 5, Fetch API)          │

└────────────┬──────────────────────────┬────────────────────┘

│ OIDC (PKCE)              │ HTTPS REST(JSON)

┌────────────▼─────────┐    ┌──────────▼─────────────────────┐

│ Keycloak             │    │ Web/AP サーバ (Spring Boot 4)   │

│ (認証・認可)          │    │  Controller→Service→Repository  │

│  Realm/Clients       │◀──▶│  Spring Security (OAuth2 RS)    │

│  ユーザー資格情報     │ JWT│  TenantContext / Hibernate Filter│

└──────────────────────┘    │  監査ログ / メール通知           │

└────────────┬────────────────────┘

│ JDBC

┌────────────▼────────────────────┐

│  MySQL 8.x (DB)                  │

│  + 既存usersテーブル(参照のみ)    │

└──────────────────────────────────┘

### 2.3 物理構成図

本番環境はクラウド(AWS等)上にDocker化したアプリを配置する。

| コンポーネント | 構成 | 備考 |
|---|---|---|
| ロードバランサ | Application Load Balancer | HTTPS終端、ACM証明書、WAF連携 |
| AP実行基盤 | ECS on Fargate / 2vCPU・4GB タスク × 2以上 | Auto Scaling(CPU 70%閾値)、Blue/Greenデプロイ |
| Keycloak | ECS on Fargate / 2vCPU・4GB × 2 | 本プロジェクトで構築、共有RDS(専用スキーマ) |
| コンテナレジストリ | Amazon ECR | Spring Boot AP / Keycloak のDockerイメージ |
| AP用DB | Amazon RDS for MySQL 8.x / Multi-AZ | 本番のみMulti-AZ、ステージングはSingle-AZ、KMS暗号化 |
| 既存users用DB | 既存RDS MySQL | ReadOnlyレプリカ経由で参照(本システムからwriteしない) |
| メール送信 | Amazon SES | 通知メール・招待メールに使用 |
| 静的コンテンツ | S3 + CloudFront | HTML/CSS/JS の配信 |
| シークレット管理 | AWS Secrets Manager | DBパスワード、Keycloak管理者資格情報、KMS鍵 |
| VPC構成 | Public / Private Subnet | ALBはPublic、ECS/RDSはPrivate、NAT GW経由で外部API |

### 2.4 ソフトウェア構成

| 分類 | プロダクト/バージョン | 用途 |
|---|---|---|
| JDK | Eclipse Temurin 21 (LTS) | Javaランタイム |
| Webフレームワーク | Spring Boot 4.x | アプリケーション基盤 |
| セキュリティ | Spring Security 6.x (OAuth2 Resource Server) | JWT検証・認可 |
| 認証基盤 | Keycloak 最新安定版 | OIDC Provider、ユーザー管理 |
| ORM | Spring Data JPA / Hibernate 7.x | DBアクセス |
| DBドライバ | MySQL Connector/J 9.x | DB接続 |
| バリデーション | Jakarta Bean Validation | 入力検証 |
| ロギング | SLF4J + Logback (JSON出力) | ログ出力 |
| DBマイグレーション | Flyway 10.x | スキーマ変更管理 |
| ビルド | Gradle 8.x (Kotlin DSL) | ビルド・依存管理 |
| フロント | Bootstrap 5.3 / Vanilla JS | UI |
| テナント分離 | Hibernate Filter + AOPによる自動付与 | tenant_id絞込 |

## 3. 画面設計

### 3.1 画面遷移図

┌─────────────────┐

│  ログイン画面   │

└────────┬────────┘

│ 認証成功

┌────────▼────────┐

┌────────┤ ダッシュボード  ├────────┐

│        └────────┬────────┘        │

│                 │                  │

┌───────▼───────┐ ┌───────▼───────┐ ┌──────▼──────┐

│ タスク一覧    │ │ タスク詳細    │ │ ユーザー管理 │

│ /tasks        │ │ /tasks/{id}   │ │ /admin/users│

└───────┬───────┘ └───────┬───────┘ └─────────────┘

│                 │

┌───────▼───────┐ ┌───────▼───────┐

│ タスク登録/編 │ │ テナント切替  │

│ /tasks/new    │ │ /tenants/sel  │

└───────────────┘ └───────────────┘

### 3.2 画面一覧

| No. | 画面名 | URL | 概要 |
|---|---|---|---|
| S-01 | ログイン | /login | Keycloakへリダイレクト(OIDC) |
| S-02 | OIDC認可コールバック | /callback | 認可コード→JWT交換 |
| S-03 | ダッシュボード | / | 当日件数・期限切れ件数・自分の進捗 |
| S-04 | タスク一覧 | /tasks | 日付ピッカー、期限切れ常時表示、絞り込み |
| S-05 | タスク詳細 | /tasks/{id} | 1件のタスク内容表示・関係者一覧 |
| S-06 | タスク登録/編集 | /tasks/new, /tasks/{id}/edit | 新規・編集兼用(編集は所有者のみ) |
| S-07 | テナント切替 | /tenants/select | 所属テナントから選択 |
| S-08 | ユーザー管理(テナント) | /tenant/users | Tenant Adminのみ。招待・ロール変更 |
| S-09 | プロフィール | /me | 既存usersから氏名・部署を表示(読み取り専用) |
| S-10 | 通知設定 | /me/notifications | メール通知ON/OFF |
| S-11 | テナント管理(SaaS) | /admin/tenants | SaaS運営者のみ。テナントCRUD |

### 3.3 主要画面仕様

#### 3.3.1 タスク一覧画面(S-04)

| 項目 | 仕様 |
|---|---|
| 日付ピッカー | 画面上部。デフォルトはTODAY。前日/翌日ボタン併設。「今日に戻る」ボタン |
| 期限切れセクション | 日付フィルタの上に常時表示。期限切れ未完了タスクのみを赤色強調で一覧表示 |
| 本日(または選択日)セクション | 選択日を期限とするタスクを表示 |
| 上部フィルタ | ステータス・所有者・担当者・優先度・公開範囲・キーワード |
| 並び替え | 期限/優先度/作成日(昇順・降順) |
| 一覧項目 | タイトル / ステータス / 所有者 / 担当者 / 期限 / 優先度 / 公開範囲アイコン |
| 行操作 | クリックで詳細遷移、チェックボックスで完了化(所有者/担当者のみ) |
| 編集ボタン | 所有者またはTenant Adminにのみ表示 |
| 新規追加 | 右上「+ 新規」ボタンで S-06 へ遷移(初期値: 期限=表示中の日付) |
| ページング | 本日セクション・期限切れセクションそれぞれ50件単位 |

#### 3.3.2 タスク登録/編集画面(S-06)

- タイトル(必須、100文字)
- 説明(任意、2000文字、Markdownサポート将来検討)
- 期限日(必須、カレンダーピッカー)
- 担当者(任意、プルダウン、テナント内ユーザーから選択)
- 優先度(必須、ラジオボタン: 高/中/低)
- ステータス(プルダウン: 未着手/進行中/完了/保留)
- 公開範囲(必須、ラジオボタン: 全公開/関係者限定/非公開)
- 関係者リスト(公開範囲が「関係者限定」のときのみ活性、テナント内ユーザーから複数選択可)
- 編集画面では所有者(=ログインユーザー)以外はフィールドが読み取り専用

#### 3.3.3 ダッシュボード画面(S-03)

- 当日の期限タスク件数(大きく表示)
- 期限超過(未完了)タスク件数
- 本日完了したタスク件数
- 自分が所有するタスクの進捗グラフ(完了/進行中/未着手の円グラフ)
- 優先度別の件数バーチャート

## 4. データベース設計

### 4.1 ER図

主要エンティティのリレーションを以下に示す。

┌─────────────┐         ┌──────────────┐         ┌─────────────────────┐

│   tenants   │ 1     N │ user_tenants │ N     1 │ users (既存・参照のみ)│

│ (テナント)  │────────<│ (所属+role)  │>────────│ (oidc_sub等)        │

└──────┬──────┘         └──────────────┘         └──────────┬──────────┘

│ 1                                                  │ 1

│ N                                                  │ N

┌──────▼─────────────────────────────────┐                  │

│              tasks                     │ N              1 │

│ (tenant_id, owner_id, visibility,      │<─────────────────┘

│  due_date NOT NULL, assignee_id 等)    │ N

└──────┬──────────────────────────────────┘ │

│ 1                                  │

│ N                                  │

┌──────▼────────────┐                       │

│ task_stakeholders │ N      1              │

│ (関係者リスト)    │>──────────────────────┘

└───────────────────┘

全テーブルが tenant_id 列を保持し、Hibernate Filter で自動絞込

カテゴリ・チーム関連テーブルは将来追加(本フェーズでは作成しない)

┌────────────────────┐

│    audit_logs      │

│    (監査ログ)      │

└────────────────────┘

### 4.2 テーブル定義(MySQL 8.x / utf8mb4_unicode_ci)

既存usersテーブルは構造を変更しない。本システムが新規に作成するテーブルは、すべて tenant_id 列を持ち、Hibernate Filter で自動絞込する。

#### 4.2.1 users (既存・参照のみ・変更不可)

| カラム名 | 型 | PK | NOT NULL | 備考 |
|---|---|---|---|---|
| id | BIGINT AUTO_INCREMENT | ○ | ○ | 主キー(変更不可) |
| oidc_sub | VARCHAR(255) |   | ○ | Keycloak Subject(UNIQUE) |
| email | VARCHAR(255) |   | ○ | メールアドレス |
| full_name | VARCHAR(255) |   | ○ | 氏名 |
| full_name_kana | VARCHAR(255) |   | ○ | 氏名カナ |
| department_name | VARCHAR(255) |   |   | 部署名 |

#### 4.2.2 tenants (テナント)

| カラム名 | 型 | PK | NOT NULL | 備考 |
|---|---|---|---|---|
| id | BIGINT AUTO_INCREMENT | ○ | ○ | 主キー |
| code | VARCHAR(50) |   | ○ | テナント識別コード(UNIQUE) |
| name | VARCHAR(255) |   | ○ | テナント名 |
| plan | VARCHAR(20) |   | ○ | FREE/STANDARD/ENTERPRISE |
| status | VARCHAR(20) |   | ○ | ACTIVE/SUSPENDED/DELETED |
| created_at | DATETIME |   | ○ | 作成日時 |
| updated_at | DATETIME |   | ○ | 更新日時 |

#### 4.2.3 user_tenants (ユーザー所属・ロール)

| カラム名 | 型 | PK | NOT NULL | 備考 |
|---|---|---|---|---|
| user_id | BIGINT | ○ | ○ | FK: users.id |
| tenant_id | BIGINT | ○ | ○ | FK: tenants.id |
| role | VARCHAR(20) |   | ○ | TENANT_ADMIN/MEMBER(参照のみのVIEWERは設けない、要件定義書 §2.3 に整合) |
| status | VARCHAR(20) |   | ○ | ACTIVE/INVITED/DISABLED |
| joined_at | DATETIME |   | ○ | 参加日時 |

#### 4.2.4 tasks (タスク)

| カラム名 | 型 | PK | NOT NULL | 備考 |
|---|---|---|---|---|
| id | BIGINT AUTO_INCREMENT | ○ | ○ | 主キー |
| tenant_id | BIGINT |   | ○ | テナント分離キー |
| title | VARCHAR(100) |   | ○ | タイトル |
| description | TEXT |   |   | 説明(2000文字以内) |
| status | VARCHAR(20) |   | ○ | 未着手/進行中/完了/保留 |
| priority | VARCHAR(10) |   | ○ | HIGH/MEDIUM/LOW |
| visibility | VARCHAR(20) |   | ○ | TENANT/STAKEHOLDERS/PRIVATE(デフォルトTENANT) |
| owner_id | BIGINT |   | ○ | FK: users.id (所有者)。編集・削除権限の根拠 |
| assignee_id | BIGINT |   |   | FK: users.id (担当者・任意) |
| due_date | DATE |   | ○ | 期限日(必須) |
| completed_at | DATETIME |   |   | 完了日時 |
| deleted_at | DATETIME |   |   | 論理削除日時(NULL=有効) |
| created_at | DATETIME |   | ○ | 作成日時 |
| updated_at | DATETIME |   | ○ | 更新日時 |

- 将来カテゴリ機能・チーム機能を追加する余地として、tasks に `category_id` / `team_id` 列を後で追加可能な設計とする

#### 4.2.5 task_stakeholders (タスク関係者)

公開範囲が STAKEHOLDERS のタスクで、所有者が指名した関係者を保持する。

| カラム名 | 型 | PK | NOT NULL | 備考 |
|---|---|---|---|---|
| task_id | BIGINT | ○ | ○ | FK: tasks.id |
| user_id | BIGINT | ○ | ○ | FK: users.id |
| tenant_id | BIGINT |   | ○ | テナント分離(検索高速化) |
| added_by | BIGINT |   | ○ | 追加した所有者(監査用) |
| added_at | DATETIME |   | ○ | 追加日時 |

#### 4.2.6 audit_logs (監査ログ)

| カラム名 | 型 | PK | NOT NULL | 備考 |
|---|---|---|---|---|
| id | BIGINT AUTO_INCREMENT | ○ | ○ | 主キー |
| tenant_id | BIGINT |   |   | テナント分離(NULL=システム横断) |
| user_id | BIGINT |   |   | 操作ユーザー(認証失敗時はNULL) |
| action | VARCHAR(50) |   | ○ | LOGIN/CREATE/UPDATE/DELETE 等 |
| entity_type | VARCHAR(50) |   |   | 対象エンティティ種別 |
| entity_id | BIGINT |   |   | 対象ID |
| detail | JSON |   |   | 変更内容(差分) |
| ip_address | VARCHAR(45) |   |   | IPv4/IPv6 |
| hash_chain | CHAR(64) |   | ○ | 前レコードのSHA-256(改ざん検知用) |
| created_at | DATETIME |   | ○ | 発生日時 |

#### 4.2.7 user_notification_settings (ユーザー通知設定)

ユーザー単位 × テナント単位の通知設定。期限当日メール通知(F-18)、期限超過リマインド、関係者登録時の通知の各種フラグを保持する。

| カラム名 | 型 | PK | NOT NULL | 備考 |
|---|---|---|---|---|
| user_id | BIGINT | ○ | ○ | 既存users.id |
| tenant_id | BIGINT | ○ | ○ | tenants.id |
| email_due_today | BOOLEAN |   | ○ | 期限当日メール通知(デフォルト TRUE) |
| email_overdue | BOOLEAN |   | ○ | 期限超過メール通知(デフォルト TRUE) |
| email_stakeholder | BOOLEAN |   | ○ | 関係者として登録された時の通知(デフォルト TRUE) |
| updated_at | DATETIME |   | ○ | 更新日時 |

#### 4.2.8 shedlock (運用テーブル / バッチ排他制御)

[ShedLock](https://github.com/lukas-krecan/ShedLock) 標準スキーマ。複数の ECS Fargate タスクで同時起動するスケジュールバッチを排他制御するために使用する。アプリケーションロジックからは直接参照しない。

| カラム名 | 型 | PK | NOT NULL | 備考 |
|---|---|---|---|---|
| name | VARCHAR(64) | ○ | ○ | ロック名(バッチごとに一意) |
| lock_until | TIMESTAMP(3) |   | ○ | ロック解放予定時刻 |
| locked_at | TIMESTAMP(3) |   | ○ | ロック取得時刻 |
| locked_by | VARCHAR(255) |   | ○ | ロック取得インスタンス識別子 |

### 4.3 インデックス設計

| テーブル.カラム | 目的 |
|---|---|
| users.oidc_sub (UNIQUE) | ログイン時にKeycloakのsubと突合(既存) |
| tenants.code (UNIQUE) | テナント識別コードからの検索 |
| user_tenants(tenant_id, user_id) | テナント切替時のロール解決 |
| tasks(tenant_id, due_date) | 当日タスク表示・日付切替の主クエリ |
| tasks(tenant_id, status, due_date) | 期限切れ未完了タスク常時表示 |
| tasks(tenant_id, owner_id, due_date) | 所有者ビュー(編集可能なタスク) |
| tasks(tenant_id, assignee_id, due_date) | 担当者の期限順表示 |
| tasks(tenant_id, visibility) | 公開範囲フィルタ |
| tasks(tenant_id, deleted_at) | 論理削除フィルタ |
| task_stakeholders(user_id, tenant_id) | ユーザーが関係者となっているタスクの参照 |
| audit_logs(tenant_id, user_id, created_at) | テナント別監査ログ検索 |

### 4.4 主要 CREATE TABLE 抜粋

-- テナント

CREATE TABLE tenants (

id          BIGINT       NOT NULL AUTO_INCREMENT,

code        VARCHAR(50)  NOT NULL,

name        VARCHAR(255) NOT NULL,

plan        VARCHAR(20)  NOT NULL DEFAULT 'STANDARD',

status      VARCHAR(20)  NOT NULL DEFAULT 'ACTIVE',

created_at  DATETIME     NOT NULL,

updated_at  DATETIME     NOT NULL,

PRIMARY KEY (id),

UNIQUE KEY uq_tenants_code (code)

) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- ユーザーのテナント所属とロール

CREATE TABLE user_tenants (

user_id    BIGINT      NOT NULL,

tenant_id  BIGINT      NOT NULL,

role       VARCHAR(20) NOT NULL,

status     VARCHAR(20) NOT NULL DEFAULT 'ACTIVE',

joined_at  DATETIME    NOT NULL,

PRIMARY KEY (user_id, tenant_id),

KEY idx_ut_tenant (tenant_id, user_id),

CONSTRAINT fk_ut_user   FOREIGN KEY (user_id)   REFERENCES users(id),

CONSTRAINT fk_ut_tenant FOREIGN KEY (tenant_id) REFERENCES tenants(id)

) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- タスク(due_date必須、owner_id, visibility)

CREATE TABLE tasks (

id           BIGINT       NOT NULL AUTO_INCREMENT,

tenant_id    BIGINT       NOT NULL,

title        VARCHAR(100) NOT NULL,

description  TEXT         NULL,

status       VARCHAR(20)  NOT NULL,

priority     VARCHAR(10)  NOT NULL,

visibility   VARCHAR(20)  NOT NULL DEFAULT 'TENANT',

owner_id     BIGINT       NOT NULL,

assignee_id  BIGINT       NULL,

due_date     DATE         NOT NULL,

completed_at DATETIME     NULL,

deleted_at   DATETIME     NULL,

created_at   DATETIME     NOT NULL,

updated_at   DATETIME     NOT NULL,

PRIMARY KEY (id),

KEY idx_tasks_tenant_due       (tenant_id, due_date),

KEY idx_tasks_tenant_owner_due (tenant_id, owner_id, due_date),

KEY idx_tasks_tenant_assignee  (tenant_id, assignee_id, due_date),

KEY idx_tasks_tenant_status_due(tenant_id, status, due_date),

CONSTRAINT fk_tasks_tenant FOREIGN KEY (tenant_id) REFERENCES tenants(id),

CONSTRAINT fk_tasks_owner  FOREIGN KEY (owner_id)  REFERENCES users(id)

) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- タスク関係者(visibility=STAKEHOLDERS のとき参照可)

CREATE TABLE task_stakeholders (

task_id    BIGINT   NOT NULL,

user_id    BIGINT   NOT NULL,

tenant_id  BIGINT   NOT NULL,

added_by   BIGINT   NOT NULL,

added_at   DATETIME NOT NULL,

PRIMARY KEY (task_id, user_id),

KEY idx_ts_user_tenant (user_id, tenant_id),

CONSTRAINT fk_ts_task FOREIGN KEY (task_id) REFERENCES tasks(id),

CONSTRAINT fk_ts_user FOREIGN KEY (user_id) REFERENCES users(id)

) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

## 5. API設計

### 5.1 API一覧

| No. | メソッド | パス | 概要 |
|---|---|---|---|
| A-01 | GET | /api/auth/me | ログイン中ユーザー情報・所属テナント一覧 |
| A-02 | POST | /api/auth/tenants/{id}/select | アクティブテナント切替 |
| A-03 | POST | /api/auth/logout | Keycloakセッション終了 |
| A-04 | GET | /api/tenants | テナント一覧 (SaaS Admin) |
| A-05 | POST | /api/tenants | テナント作成 (SaaS Admin) |
| A-06 | PUT | /api/tenants/{id} | テナント更新 (SaaS Admin) |
| A-07 | GET | /api/users/me | 自身のプロフィール(既存users参照) |
| A-08 | GET | /api/tenant/users | テナント内ユーザー一覧 |
| A-09 | POST | /api/tenant/users/invite | ユーザー招待 (Tenant Admin) |
| A-10 | PUT | /api/tenant/users/{id}/role | ロール変更 (Tenant Admin) |
| A-11 | GET | /api/tasks | タスク一覧(絞り込み可) |
| A-12 | GET | /api/tasks/{id} | タスク詳細取得 |
| A-13 | POST | /api/tasks | タスク作成 |
| A-14 | PUT | /api/tasks/{id} | タスク更新 |
| A-15 | DELETE | /api/tasks/{id} | タスク論理削除(所有者またはTenant Admin) |
| A-16 | PATCH | /api/tasks/{id}/status | ステータス変更(所有者または担当者) |
| A-17 | PATCH | /api/tasks/{id}/visibility | 公開範囲変更(所有者のみ) |
| A-18 | GET | /api/tasks/{id}/stakeholders | 関係者一覧 |
| A-19 | POST | /api/tasks/{id}/stakeholders | 関係者追加(所有者のみ) |
| A-20 | DELETE | /api/tasks/{id}/stakeholders/{userId} | 関係者削除(所有者のみ) |
| A-21 | GET | /api/dashboard/summary | ダッシュボード集計(当日件数・期限切れ件数) |
| A-22 | GET | /api/audit-logs | 監査ログ参照 (Tenant Admin) |
| A-23 | GET | /api/users/me/notification-settings | 自身の通知設定取得(現テナント) |
| A-24 | PUT | /api/users/me/notification-settings | 自身の通知設定更新(現テナント) |

### 5.2 認証方式(Keycloak / OIDC)

- 認証はKeycloakのOIDC(認可コードフロー + PKCE)に委譲
- Keycloak が発行するJWTを Spring Security OAuth2 Resource Server で検証
- アクセストークン有効期限: 15分、リフレッシュトークン: 8時間(NIST SP 800-63B AAL2想定)
- API呼出時は HTTPヘッダ Authorization: Bearer {access_token} を付与
- ログイン直後に oidc_sub を usersテーブルと突合し、内部ユーザーIDを解決
- アクティブテナントはCookieまたは X-Tenant-Id ヘッダで送出、毎リクエストで user_tenants の権限を確認

#### 5.2.1 認証フロー

[1] ブラウザ ──(GET /)──> AP

[2] AP ──(302 認可エンドポイント)──> Keycloak

[3] Keycloak ── ログイン画面表示・認証 ──> ブラウザ

[4] Keycloak ──(認可コード)──> ブラウザ ──> AP /callback

[5] AP ──(コード+PKCE)──> Keycloak ──(JWT)──> AP

[6] AP: JWT.sub を users.oidc_sub と突合、user_tenants からロール解決

[7] AP ──(セッションCookie)──> ブラウザ

[8] 以降のAPI呼出: Authorization: Bearer ... + X-Tenant-Id: {tenant_id}

### 5.3 主要APIスキーマ

#### 5.3.1 GET /api/auth/me

レスポンス(200 OK):

{

"user": {

"id":             1,

"email":          "taro@example.com",

"fullName":       "山田 太郎",

"departmentName": "営業部"

},

"tenants": [

{ "id": 11, "code": "acme", "role": "TENANT_ADMIN" },

{ "id": 12, "code": "beta", "role": "MEMBER" }

],

"activeTenantId": 11

}

#### 5.3.2 GET /api/tasks

クエリパラメータ:

| パラメータ | 型 | 説明 |
|---|---|---|
| targetDate | date | 表示対象日(省略時は当日 TODAY) |
| includeOverdue | boolean | 期限切れ未完了タスクを含めるか(デフォルトtrue) |
| status | string | ステータス(複数指定可) |
| ownerId | long | 所有者ID |
| assigneeId | long | 担当者ID |
| priority | string | HIGH/MEDIUM/LOW |
| visibility | string | TENANT/STAKEHOLDERS/PRIVATE |
| keyword | string | タイトル・説明部分一致 |
| page | int | ページ番号(0始まり) |
| size | int | 1ページ件数(デフォルト50) |
| sort | string | 例: dueDate,asc |

レスポンスはSpring Pageable形式(content / totalElements / totalPages / size)。tenant_idは応答に含めず、サーバ側で必ずフィルタする。

#### 5.3.3 POST /api/tasks

リクエスト:

{

"title":       "設計レビュー",

"description": "基本設計書のレビュー",

"assigneeId":  2,

"dueDate":     "2026-05-20",

"priority":    "HIGH",

"visibility":  "STAKEHOLDERS",

"stakeholderUserIds": [3, 5, 8]

}

tenant_id・owner_id はリクエストで受け取らず、認証コンテキストから自動付与する。dueDate は必須。

### 5.4 エラー応答

全API共通のエラー応答形式:

{

"timestamp": "2026-05-05T01:00:00Z",

"status":    400,

"error":     "Bad Request",

"code":      "E_VALIDATION",

"message":   "タイトルは必須です",

"path":      "/api/tasks"

}

| HTTPステータス | code | 意味 |
|---|---|---|
| 400 | E_VALIDATION | 入力検証エラー |
| 401 | E_UNAUTHORIZED | 未認証 / トークン無効 |
| 403 | E_FORBIDDEN | 権限なし / 他テナント参照 |
| 404 | E_NOT_FOUND | リソース未存在 |
| 409 | E_CONFLICT | 一意制約違反など |
| 500 | E_INTERNAL | サーバ内部エラー |

## 6. セキュリティ設計(NIST準拠)

### 6.1 NIST統制マッピング

| NIST統制 | 本システムでの実装 | 備考 |
|---|---|---|
| AC-2 アカウント管理 | Keycloak + user_tenants | 招待→承認のフロー |
| AC-3 アクセス制御 | Spring Security @PreAuthorize | ロールベース |
| AC-4 情報フロー | tenant_id による分離 | Hibernate Filter自動付与 |
| AU-2 監査ログ | audit_logs テーブル | 1年保管 |
| AU-10 否認防止 | ハッシュチェーン | 改ざん検知 |
| IA-2 識別と認証 | Keycloak OIDC + JWT | PKCE使用 |
| IA-5 認証情報管理 | Keycloak側でSP 800-63B | MFA推奨 |
| SC-8 通信保護 | TLS 1.3 | HSTS有効 |
| SC-13 暗号化 | AES-256(KMS) | 保存時暗号化 |
| SI-10 入力検証 | Jakarta Bean Validation | サーバ側で必ず検証 |

### 6.2 認証・認可

- Keycloak経由のOIDC認証、Spring Security OAuth2 Resource Server でJWT検証
- ロールはテナントスコープで本システム内 user_tenants に保持
- @PreAuthorize でメソッドレベル認可、TenantContextでテナント絞込を強制
- SaaS Admin / Tenant Admin / Member の3階層(Viewerは設けない)

#### 6.2.1 タスクごとの認可ロジック

- 参照: 公開範囲 = TENANT なら同テナント全員、STAKEHOLDERS なら所有者+担当者+task_stakeholders、PRIVATE なら所有者のみ。Tenant Admin は常に参照可
- 編集: 所有者のみ。ただし Tenant Admin は強制編集可(監査ログに記録)
- 削除: 所有者または Tenant Admin
- ステータス変更: 所有者・担当者・Tenant Admin
- 公開範囲変更・関係者編集: 所有者のみ
- 関係者一覧の参照(`GET /api/tasks/{id}/stakeholders`): タスクを参照可能なユーザー全員

#### 6.2.2 ダッシュボード集計の認可スコープ

ダッシュボード(F-17 / `GET /api/dashboard/summary`)は、§6.2.1 のタスク参照認可フィルタを適用した後のタスク集合に対して件数集計を行う。

- 即ち、PRIVATE タスクは所有者のダッシュボードでのみ件数加算される
- STAKEHOLDERS タスクは「所有者・担当者・関係者リスト登録ユーザー」にのみ件数加算される
- TENANT タスクはテナント全員に同一件数で加算される
- Tenant Admin にはテナント内全タスクが加算される

**根拠**: NIST AC-4(情報フロー保護)。集計件数からも他者の PRIVATE タスクの存在を推定させない。

#### 6.2.3 認可違反時の応答ポリシー

認可違反は HTTPステータスを使い分けることで、情報漏洩経路を最小化する。

| 違反種別 | HTTPステータス | 理由 |
|---|---|---|
| 未認証(JWT不正・期限切れ) | **401** Unauthorized | 標準 |
| 参照系(GET)で参照不可 | **404** Not Found | リソースの存在自体を漏らさない(NIST AC-4) |
| 更新系(PUT/PATCH/DELETE)で権限不足 | **403** Forbidden | 編集権限が無いことを明示 |
| テナント越境アクセス | **403** Forbidden | `X-Tenant-Id` と認可コンテキストの不一致は明示的に拒否 |
| ロールベース不可(SaaS Admin専用APIなど) | **403** Forbidden | 認証済だが権限が無い |

**実装方針**: `TaskOwnershipException`(403) と `TaskNotViewableException`(404)を分離し、`@RestControllerAdvice` で対応する HTTPステータスにマッピングする。違反は監査ログ(action=`*_DENIED`)に必ず記録する。

### 6.3 マルチテナント分離

- 全業務テーブルに tenant_id 列を必須化、Hibernate Filter で全SELECT/UPDATEに自動条件付与
- DBレベルで tenant_id を含む複合インデックスを必ず作成
- 結合テストでクロステナント漏洩テストを必須化

### 6.4 入力検証・出力エンコーディング

- Jakarta Bean Validationで全入力をサーバ側検証(SI-10)
- HTML出力は自動エスケープ(XSS対策)
- JPAのパラメータバインディング使用(SQLインジェクション対策)

### 6.5 通信・データ保護

- HTTPS必須(TLS 1.3、最低 TLS 1.2)、HSTSヘッダ送出
- 認証情報は本システムに保存しない(Keycloakが一元管理)
- DB接続はSSL/TLS有効化、RDSはKMS暗号化
- 機密情報は AWS Secrets Manager で管理

### 6.6 CSRF/CORS

- API はステートレスのためCSRFトークンは無効化、JWT検証で代替
- CORSは許可Origin をホワイトリスト管理

### 6.7 監査ログの改ざん検知

- audit_logs.hash_chain は前レコードのid+detail+created_atのSHA-256
- バッチで日次整合性チェックを実行、不整合時はアラート

## 7. 例外・ログ設計

### 7.1 例外設計

- 業務例外(BusinessException)とシステム例外(SystemException)を分離
- @RestControllerAdvice で集中ハンドリング
- 業務例外は4xx応答、システム例外は5xx応答
- クロステナントアクセスは TenantAccessDeniedException として403を返却
- タスクの編集権限違反は TaskOwnershipException として403を返却
- タスクの参照権限違反は TaskNotViewableException として404を返却(リソース存在を漏らさない)

### 7.2 ログ設計

| 種類 | 出力先 | 保管 | 内容 |
|---|---|---|---|
| アプリログ | CloudWatch Logs | 30日 | INFO以上、JSON形式 |
| アクセスログ | ALBアクセスログ(S3) | 90日 | リクエスト・レスポンス情報 |
| 監査ログ | DB(audit_logs) | 1年 | 認証・データ変更の記録 |
| エラーログ | CloudWatch Logs | 180日 | WARN/ERROR/FATAL |

- 全ログにtenant_idと相関ID(traceId)を含める

## 8. バッチ設計

### 8.1 バッチ一覧

| No. | バッチ名 | 実行時刻 | 概要 |
|---|---|---|---|
| B-01 | 期限当日通知メール | 毎日 09:00 | 当日期限のタスクを担当者・関係者へメール通知 |
| B-02 | 期限超過リマインド | 毎日 09:30 | 未完了かつ期限超過タスクを所有者・担当者に通知 |
| B-03 | 監査ログ削除 | 毎月 01日 02:00 | 1年以上前の監査ログを削除 |
| B-04 | DBバックアップ | 毎日 03:00 | RDS自動スナップショット |
| B-05 | 監査ログ整合性検証 | 毎日 02:00 | ハッシュチェーンの整合性を検証 |
| B-06 | テナント使用量集計 | 毎日 04:00 | 課金用にタスク数・ユーザー数を集計 |

### 8.2 実装方針

- Spring の @Scheduled アノテーションで実装、ECS Fargate のスケジュールタスクとは別運用も検討
- 複数インスタンス起動時はShedLockで排他制御(本書 §4.2.8 参照)
- バッチも tenant_id ループで処理し、テナント分離を遵守
- 失敗時はSlack/メールでアラート(将来対応)
