spring:
  application:
    name: tasks-webapi
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info
