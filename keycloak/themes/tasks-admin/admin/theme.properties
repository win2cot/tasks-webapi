# tasks-admin: Admin Console theme for tasks-webapi (issue #729).
# Inherits the stock Keycloak v2 admin console and only overrides the message
# bundle so the tasks-webapi User Storage SPI config and the Custom User Profile
# attribute (full_name_kana) render with localized labels instead of raw keys.
parent=keycloak.v2
import=common/keycloak
